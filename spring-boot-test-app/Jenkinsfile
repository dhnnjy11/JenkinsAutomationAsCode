pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'check docker version...'
                echo 'docker version'

            } 
        }
    }
}
