pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'check docker version...'
                sh 'docker version'

            } 
        }
    }
}
